# ============================================
# PSM-SafetyTwin SVC-01: pid-parser
# P&ID 디지털화 엔진 의존성
# 라이선스: 모두 Apache 2.0 / MIT / BSD 호환
# ============================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# --- D-Fine 심볼 감지 (Apache 2.0) ---
torch>=2.1.0
torchvision>=0.16.0
supervision>=0.25.0       # 감지 결과 시각화/후처리
huggingface-hub>=0.20.0   # D-Fine 사전학습 가중치 다운로드

# --- PaddleOCR 텍스트 추출 (Apache 2.0) ---
# paddlepaddle>=2.6.0     # GPU 환경에서 설치
# paddleocr>=2.8.0        # OCR 엔진

# --- 이미지 처리 ---
opencv-python-headless>=4.9.0
Pillow>=10.2.0
numpy>=1.26.0

# --- 데이터베이스 ---
sqlalchemy>=2.0.0
asyncpg>=0.30.0           # PostgreSQL 비동기 드라이버
neo4j>=5.26.0             # 그래프 DB 드라이버

# --- 데이터 처리 ---
pydantic>=2.6.0
pandas>=2.2.0
openpyxl>=3.1.0           # Excel 내보내기

# --- 유틸리티 ---
python-dotenv>=1.0.0
loguru>=0.7.0             # 구조화 로깅
httpx>=0.27.0             # 비동기 HTTP 클라이언트 (공공데이터 API)
celery>=5.4.0             # 비동기 작업 큐 (대량 도면 처리)
redis>=5.0.0              # Celery 브로커

# --- 테스트 ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
